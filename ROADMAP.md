# 夏夜工具集 · 新工具路线图

> 工作方式：**一次只做一个，做好一个部署 + git + 归档一个，做完划掉一个**。
> 全部工具保持站点调性：纯前端、零依赖优先、数据不出浏览器。

## 执行顺序（先易后难，大项目殿后）

| # | 工具 | 内容 | 复杂度 | 依赖 | 状态 |
|---|------|------|--------|------|------|
| 1 | 文字统计 | 字数/字符/行数/中英文/阅读时长/关键词词频 | 小 | 无 | ✅ 2026-08-14 |
| 2 | 日期计算器 | 日期差 / 加 N 天 / 倒计时 / 农历（复用 lunar.js） | 小-中 | 复用现有 lunar.js | ✅ 2026-08-14 |
| 3 | IP 地址工具 | 本机 IP + 子网计算（网络/广播/主机范围）+ 校验 | 中 | 公网 IP 需第三方 API | ✅ 2026-08-14 |
| 4 | 文本批量处理 | 多行列表去重/排序/反转/大小写/加前后缀/批量替换 | 小-中 | 无 | ✅ 2026-08-14 |
| 5 | 亲戚称呼计算器 | 关系链 → 称呼（含反向「我该叫他什么」） | 中-大 | 规则表需网络查证 | ✅ 2026-08-14 |
| 6 | 像素画制作器 | 网格画布 + 色板 + 画笔/橡皮/填充 + 导出 PNG + 图片转像素风 | 大 | 无 | ✅ 2026-08-14（先做简陋版：铅笔/橡皮/选色/撤销/导出，大版本待续） |
| 7 | 图片工坊（线上 PS） | 统一大界面：上传→编辑→导出，集成+扩展图片能力 | 最大，分阶段 | 阶段二起评估 | ⬜ |

---

## 1. 文字统计

**内容**：粘贴/输入文本，实时统计——
- 总字数、中文字数、英文单词数、数字、标点数
- 字符数（含/不含空格）、行数、段落数
- 预计阅读时长（按 ~300 字/分钟）
- Top N 高频词 + 词频条形图（中英文分词）

**实现**：纯 JS 字符串处理。中文计数 `/[一-龥]/` 正则；词频用 `split + Map` 统计再排序；阅读时长 = 字数 ÷ 300。无任何依赖。

**验收**：输入「你好 hello 你好」→ 中文 2、英文 1 单词、总字数 3；空文本全 0；大文本（10 万字）输入不卡。

---

## 2. 日期计算器

**内容**：
- 两日期相差多少天（精确）
- 起始日期 + N 天 / 月 / 年 = 结果日期（处理跨月、闰年）
- 某日期距今还有多少天（倒计时）
- 显示星期几 + **农历**（复用站点已有 lunar.js 的 solarlunar 数据）

**实现**：原生 `Date`。日期差 `(d2-d1)/86400000`；加 N 月注意「1月31日+1月→2月28日」类边界；农历直接调现有 lunar.js（solar2lunar）。这是纯前端里最稳的一块，还和页脚农历角呼应。

**验收**：2026-01-01 到 2026-01-31 = 30 天；2026-08-14 的农历 = 七月初二丙午马（与现有 lunar 节点锚点一致）。

---

## 3. IP 地址工具

**内容**：
- **本机内网 IP**（IPv4 + IPv6）
- **公网 IP**（需第三方 API，见下）
- **子网计算器**：IP + 前缀（如 `192.168.1.7/24`）→ 网络地址 / 广播地址 / 可用主机范围 / 主机数 / 子网掩码
- IPv4 地址合法性校验

**实现**：
- 内网 IP：WebRTC `RTCPeerConnection` 收集 candidate 里的 IP（纯前端，无依赖）
- 公网 IP：`fetch` 第三方 API（如 ipify，需实测**国内可达 + CORS**；用户本机 Clash 是系统代理，浏览器 fetch 走自己的路，实际效果实现时验证）
- 子网计算：IPv4 转 32 位整数做位运算（网络地址 = ip & mask，广播 = 网络 | ~mask，主机数 = 2^(32-前缀) - 2），纯本地不联网

**验收**：本机内网 IP 能列出；`192.168.1.7/24` → 网络 192.168.1.0、广播 192.168.1.255、可用 192.168.1.1~254、254 台；公网 IP 若 API 不可达则优雅降级（提示"暂无法获取公网 IP"）。

---

## 4. 文本批量处理

**内容**：多行文本列表，一键处理——
- 去重（保留首次 / 保留末次）
- 排序（升 / 降 / 数值 / 按长度）
- 行序反转
- 大小写转换 / 首字母大写
- 加前缀 / 后缀
- 批量查找替换（正则可选）
- 清理：去首尾空格 / 合并多余空格 / 去空行
- 行号

**实现**：纯 JS。`split('\n')` → Array 操作 → `join('\n')`。全部原地预览、一键复制结果。

**验收**：乱序多行 → 排序/去重后正确；替换支持正则；复制按钮可用。

---

## 5. 亲戚称呼计算器

**内容**：
- 输入关系链（如「爸爸的哥哥」、「妈妈的姐姐的丈夫」）→ 正确称呼
- 反向：选「我叫他什么」/「他叫我什么」（辈分互换）
- 常见称呼速查表

**实现**：纯前端**规则表**（数据密集型）。推荐两方案：
- 规则映射表 + 两级链组合（覆盖绝大多数日常场景，3 级部分覆盖）
- ⚠️ **规则数据必须网络查证**（像 PPT 规则一样只放查证过的真实叫法，绝不放想当然的），亲戚称呼地域差异大（北方/南方叫法不同），首版按通用普通话叫法，标注地区差异

**验收**：爸爸的哥哥=伯父/大伯、妈妈的姐姐的丈夫=姨父、爸爸的爸爸=爷爷；反向「我爸爸，他叫我」=儿子/闺女（注意性别分支）。规则表查证通过。

---

## 6. 像素画制作器（用户：慢点搞，做个大的 → 先做个简陋的）

**状态**：✅ 2026-08-14 简陋版已上线（铅笔/橡皮/选色/撤销/清空/导出 PNG，16/32 尺寸，演示爱心）。大版本（填色桶/取色器/重做/图片转像素风/导出倍数可调）待续。

**内容**：
- 网格画布：16×16 / 32×32 / 64×64 可调，格子放大显示
- 工具：铅笔 / 橡皮 / 填色桶 / 取色器 / 撤销 / 重做
- 色板：预设 + 自定义取色
- **导入图片 → 转像素风**（降采样 + 最近邻采样）
- 导出 PNG（每格一像素，放大倍数可调，如 ×8 / ×16）

**实现**：canvas 网格 + `mousedown` 拖动连续绘制；色板状态管理；导出 = 离屏 canvas 每格填色 `toBlob`；图片转像素 = 缩到网格尺寸 → 最近邻采样。无依赖。

**验收**：画 3×3 能导出 9 个像素的 PNG；图片转 16×16 像素风可见轮廓；撤销/橡皮正常。

---

## 7. 图片工坊 / 线上 PS（最大，分阶段）

**有没有搞头（务实评估）**：
- **真·PS（图层/蒙版/通道/插件体系）** = 数万行级工程，单页纯前端不现实，**不建议对标**。
- 但 **「轻量在线图片工作台」完全可行**：上传 → 预览 → 编辑 → 导出一条龙，覆盖 80% 日常需求，体验比一堆零散小工具好得多。**结论：有搞头，做"图片工坊"，不做"Photoshop"。**

**分阶段**：
- **阶段一（壳）**：工具列表加入口 → 大界面（上传区 + 画布预览 + 侧边工具栏 + 导出区）→ 先集成现有「压缩 / 格式转换」
- **阶段二（编辑）**：裁剪（自由/比例）→ 旋转/翻转 → 调色（亮度/对比度/饱和度，彩蛋同款像素技术）→ 文字水印 → 图片水印
- **阶段三（扩展）**：九宫格切图 → 拼接 → 模糊/锐化 → 滤镜（灰度/反色/怀旧）

**实现**：优先原生 canvas（保持零依赖调性）；复杂交互（拖拽选区/多点编辑）再评估 fabric.js 或 cropper.js，届时权衡"依赖 vs 工作量"。

**验收（阶段一）**：从工具列表进大界面 → 传图 → 原图/结果双预览 → 可压缩、可转格式 → 导出下载；原有 2 个小工具不回归。

---

## 归档约定
每个工具做完：本地 Playwright 验证 → 逐个 scp + md5 → 线上冒烟 → git commit+push → 更新 `server-architecture.md` 版本基线 → 对话归档到 `E:\claudecode\conversation-history\` → 划掉本表对应行。
